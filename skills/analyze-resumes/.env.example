# analyze-resumes 配置模板
# 复制本文件为 .env，填入你自己的值。.env 不会进 git（已在 .gitignore）。

# 归档根目录（和 collect-resumes 共用）
# 示例: F:/miniwanob/data/在招岗位候选人管理
ARCHIVE_ROOT=

# 评估结果输出目录（留空则用 <项目根>/notes）
NOTES_DIR=

# 评估要点文件名（一般不用改）
CRITERIA_FILENAME=评估要点.md
